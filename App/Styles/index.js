import config from '@Config'

export default {
  container: {
    flex: 1,
    backgroundColor: config.viewsBackgroundColor
  },
  modalHeader: {
    center: {
      color: '#fff',
      fontSize: 17,
      fontWeight: '600'
    }
  }
}
